@using OgnreciNotMVC.Models.EntityFramework;
@model List<Tbl_Kulupler>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<table class="table table-bordered">
        <tr>
            <th>KULÜP ID</th>
            <th>KULÜP ADI</th>
            <th>İŞLEMLER</th>
        </tr>
        <tbody>
            @foreach (var klp in Model)
            {
                <tr>
                    <td>@klp.KULUPID</td>
                    <td>@klp.KULUPAD</td>
                    <td><a href="/Kulupler/Sil/@klp.KULUPID" class="btn btn-danger">SİL</a>
                    <a href="/Kulupler/Guncelle/@klp.KULUPID" class="btn btn-Dark">Güncelle</a></td>

                </tr>
            }

        </tbody>
    </table>*@

<div class="table100 ver2 m-b-110">
    <div class="table100-head">
        <table>
            <thead>
                <tr class="row100 head">
                    <th class="cell100 column1"  style="text-align:center">KULÜP ID</th>
                    <th class="cell100 column2">KULÜP ADI</th>
                    <th class="cell100 column3">ISLEMLER</th>
                </tr>

            </thead>
        </table>
    </div>

    <div class="table100-body js-pscroll">
        <table>

            <tbody>
                @foreach (var klp in Model)
                {
                    <tr class="row100 body">
                        <td class="cell100 column1"  style="text-align:center">@klp.KULUPID</td>
                        <td class="cell100 column2">@klp.KULUPAD</td>
                        <td class="cell100 column3">
                            <a href="/Kulupler/Sil/@klp.KULUPID" class="btn btn-danger" >SİL</a>
                            <a href="/Kulupler/KulupGetir/@klp.KULUPID" class="btn btn-Dark" style="color:#ffffff">Güncelle</a>
                        </td>

                    </tr>
                }

            </tbody>
          
        </table>
    </div>
</div>
<a href="/Kulupler/YeniKulup" class="btn btn-primary" style="margin-top:-155px">Yeni Kulüp Kaydı</a>



